===========================================
DATE: 6/19/2026 (Session 11 — chart works when Binance is blocked)
-------------------------------------------
PROBLEM (from Nimith's screenshot):
- On his network Binance is blocked. The Binance WebSocket never connected, so
  the chart was empty, the order book/trades were stuck "Connecting…", and the
  "Live feed dropped" note covered the whole chart. 24h high/low/vol were "--".

FIX:
- Chart data no longer comes from Binance. binance-live.js now sources candles
  from CryptoCompare (histominute/histohour/histoday, matching the 1m..1D
  buttons), CORS-friendly and NOT blocked (same host as our news feed), with
  CoinGecko OHLC as a fallback. So candlesticks always render.
- Header 24h stats (price/change/high/low/vol) now fill from a CryptoCompare
  poll (pricemultifull) every 12s when Binance's ticker isn't delivering — so
  they're no longer stuck at "--". The forming candle is also refreshed on that
  poll. Binance's WS, when reachable, still takes over (fresher) — gated by
  lastTickerTs / lastKlineTs so the two sources don't fight.
- Order book + recent trades are Binance-WS-only (no free CORS alternative). If
  Binance is unreachable they now show a clear notice ("…comes from Binance,
  which looks blocked on this network…") instead of an endless "Connecting…",
  and they NO LONGER cover the chart. New .bx-unavail style for that note.
- Paper trading marks now feed from whichever price source is live, so it works
  even when Binance is blocked.
- Entry/Stop/TP overlay + interval switcher + trades-locked-to-10 all retained.
  Cache-busters v16 -> v17.

DECISIONS MADE:
- Decoupled the chart entirely from Binance; treat Binance as the preferred
  source for order book/trades only. CryptoCompare is the primary chart source
  because it's reachable where Binance isn't and gives true 1m/5m/15m/1h/4h/1D
  candles.

HONEST LIMITS:
- Live order book + trade tape still need Binance; on a Binance-blocked network
  those two panels show the notice (no free real-time CORS alternative exists).
  Everything else — chart, price, 24h stats, signal, paper trading — works.
- Couldn't browser-test in the sandbox; needs an in-browser look on Nimith's
  network to confirm the candles now load.

===========================================
DATE: 6/19/2026 (Session 10 — buy/sell levels on chart + trades capped)
-------------------------------------------
DONE TODAY:
- Buy/sell levels ON the candlestick chart. The TradingView EMBED is a sealed
  iframe we can't draw on, so the chart was rebuilt with TradingView's free
  Lightweight Charts library:
  * candles drawn from Binance klines (REST history, limit 300) + live updates
    from the <sym>@kline_<interval> WebSocket stream.
  * the Trade Signal's levels are drawn right on the chart as labelled dashed
    price lines — "LONG/SHORT entry" (white), "Stop loss" (red), "TP1/2/3"
    (green) — via series.createPriceLine(). app.js calls CP.live.setLevels(plan)
    whenever a plan is (re)built, so the lines track the signal + coin.
  * added an interval switcher (1m/5m/15m/1h/4h/1D), default 15m, plus a
    "<SYMBOL> · <interval>" label.
- Recent trades LOCKED at 10 (was 30): onTrade trims to 10, renderTrades slices
  10. New trades still come in on top; the list no longer grows.
- binance-live.js rewritten around the new chart; kept order book + trades +
  ticker on the same combined socket (added the kline stream). Lightweight
  Charts loads lazily from unpkg on first Trade-tab open; graceful note if it
  or the feed is blocked.
- CSS for the chart bar / interval buttons; chart height 460. Cache-busters
  v15 -> v16.

DECISIONS MADE:
- Switched chart tech from the TradingView embed to Lightweight Charts purely so
  we CAN overlay the buy/sell levels (the embed can't). Trade-off: we lose the
  embed's built-in indicator menu, but RSI/MA/MACD already live in Analysis and
  the signal. Candlesticks + the entry/stop/TP overlay is the win.

HONEST LIMITS:
- Still no browser in the sandbox — syntax verified, but the live chart render,
  the kline stream and the price-line overlay need a real-browser check.

STILL PENDING / NEXT TIME:
- Confirm in-browser that the chart loads, candles stream, and the entry/stop/TP
  lines show on the right prices. Phase-3 backend still needs hosting.

===========================================
DATE: 6/19/2026 (Session 9 — live UX fixes from Nimith's feedback)
-------------------------------------------
DONE TODAY (after deploying phases 1-3 to the live link):
- Removed the "updated Xs ago" text and the Refresh button from the top bar.
  Status now just shows "Live · <clock>".
- Stopped the page resetting on the 30s cycle:
  * binance-live.js setSymbol() no longer tears down + reconnects the WebSocket
    when the coin hasn't changed (was flickering the order book and resetting the
    Trade tab every 30s). The live numbers keep streaming in place.
  * The 30s data refresh now does a LIGHT in-place update (refreshSelectedCoin):
    updates the numbers + Trade Signal only. It does NOT reconnect the socket,
    does NOT re-point the chart, and does NOT touch the Profit Calculator. Full
    setup still runs once on first load and whenever the user changes coin.
- News feed: new headlines now ACCUMULATE on top of old ones (deduped, capped at
  60) instead of wiping the list each refresh; the renderer keeps the reading
  position when items are added on top, so scrolling/reading isn't interrupted.
- News click-to-read: clicking a headline opens a popup with the article SUMMARY
  + source + impact/sentiment tags and a link to the original — so it's readable
  even when the real link won't open. Summary text comes from the CryptoCompare
  feed's `body` (now passed through in api.js). Esc / backdrop / × close it.
- Cache-busters v14 -> v15.

DECISIONS MADE:
- Kept the 30s background refresh (so news + signal stay fresh) but made it
  non-disruptive rather than removing it. Live prices come from the 1s tick +
  the Trade tab WebSocket, which update in place.
- Left the Profit Calculator completely untouched on refresh (per Nimith).

HONEST LIMITS:
- Still can't browser-test in this sandbox; logic/syntax verified. The news
  summary needs the CryptoCompare feed (the live source on the static site) —
  headlines without a body show a "use the source link" note.

STILL PENDING / NEXT TIME:
- Nimith to confirm the live link feels stable now (no resetting) and the news
  popup reads well.
- Phase 3 backend still needs hosting to switch on the "vs expected" logic.

===========================================
DATE: 6/19/2026 (Session 8 — backend macro engine, phase 3)
-------------------------------------------
DONE TODAY:
- Added the backend macro engine so the Long/Short signal can use the
  "higher/lower than expected" logic the cheat-sheet is built on.
- server/index.js: new /api/macro endpoint returning
  * an economic calendar with FORECAST + ACTUAL (free ForexFactory-derived feed
    nfs.faireconomy.media, no key) — each US high/medium event scored as a
    surprise per the cheat-sheet (CPI/PCE/jobs/claims/rate decision).
  * optional real Fed macro TREND from FRED (free key): CPI YoY, Fed funds rate,
    unemployment, each as rising/falling.
  * optional short LLM macro read (reuses ANTHROPIC_API_KEY) -> {bias, summary}.
  All defensive (try/catch -> empty), key stays server-side.
- Frontend: js/api.js getMacro() prefers the backend, returns null on the static
  site. js/macro.js evaluate() now takes that payload and folds in: data
  surprises (weight 3.5), FRED trend drivers, upcoming events w/ forecasts as
  cautions, and the LLM summary. js/trade.js carries macroSummary + backed into
  the plan; js/render.js shows the summary line + a "live macro feed" tag.
- Graceful fallback: with no backend (GitHub Pages / file://) everything works
  exactly as phase 1 (browser-only headline-direction). Verified backward-compat
  + the backend-fed path in Node (CPI/NFP surprises -> strong LONG, summary
  threads through).
- Docs: .env.example gets FRED_API_KEY; README documents /api/macro + a hosting
  note (set CP.config.backend.base to the deployed URL). Cache-busters v13 -> v14.

DECISIONS MADE:
- Calendar source: free faireconomy JSON (US, high/medium). Honest limit: it
  reliably gives upcoming FORECASTS and exposes ACTUALs as they print; the most
  complete "actual vs forecast" history would want a paid calendar key — left as
  a future upgrade.
- Backend is OFF by default (config.backend.base ""). It only kicks in once
  server/index.js is hosted and config points at it.

HONEST LIMITS (couldn't fully verify here):
- No browser + outbound calls likely blocked in this sandbox, so the live
  /api/macro fetches (faireconomy / FRED / Anthropic) weren't exercised end to
  end. Syntax + the fold-in logic are tested; needs a real deploy to confirm the
  external feeds.

HOSTING CHECKLIST FOR NIMITH (to make phase 3 live):
1. Deploy server/index.js to Render / Railway / Fly.io (Node 18+, `npm start`).
2. Set FRED_API_KEY (free: fred.stlouisfed.org/docs/api/api_key.html). Optional:
   CRYPTOPANIC_TOKEN, ANTHROPIC_API_KEY.
3. Point the frontend at it: set CP.config.backend.base in js/config.js to the
   backend URL, bump cache-busters, redeploy the static site.
   (Or host the static site FROM that same Node server, then base "" works.)

STILL PENDING / NEXT TIME:
- Nimith to review phases 1-3 in a browser and decide on hosting for phase 3.
- Deploy to the live branch when he gives the go.

===========================================
DATE: 6/19/2026 (Session 7 — Binance-style Trade tab, phase 2)
-------------------------------------------
DONE TODAY:
- Rebuilt the Trade tab into a Binance Futures-style layout:
  * Symbol header strip — live price, 24h change, 24h high/low and 24h volume
    (base + USDT), updating from Binance's ticker stream.
  * TradingView candlestick chart (official embed) for BINANCE:<symbol>, dark
    theme; recreated when you switch coins.
  * Live ORDER BOOK (top ~14 bids/asks with depth bars) and RECENT TRADES feed,
    both streaming from Binance's public WebSocket — no API key, same feed the
    Binance site uses. Spot market data (matches the rest of the app).
  * Paper BUY/SELL panel + open POSITIONS table (kept the macro/news Trade
    Signal and the Profit Calculator below).
- New js/binance-live.js (CP.live): one combined WebSocket
  (<sym>@depth20@100ms / @aggTrade / @ticker), throttled rendering, auto
  reconnect (up to 5 tries) and a graceful "not on Binance / feed blocked"
  notice. Loads tv.js for the chart. Sockets only run while the Trade tab is
  open (opened on tab enter, closed on leave) to avoid background connections.
- New js/paper.js (CP.paper): futures-style paper trading — $10,000 fake
  balance, long/short with leverage, one position per symbol that an opposite
  order reduces/closes/flips, live unrealized PnL + ROE + liquidation price
  against the Binance mark, 0.05% fees, Reset button. Persists to localStorage
  (cp_paper_v1). Smoke-tested the open/mark/close/flip math in Node — correct.
- Wiring: app.selectCoin points CP.live at the coin; switchTab starts/stops the
  streams; paper Buy/Sell/Reset/Close buttons wired; CP.paper.init() on boot.
- index.html Trade tab markup rebuilt (kept all IDs app.js depends on:
  tradeCoinSearch/Suggest, tradeCoinLabel, tradePosSize, tradeBody, calc*).
  Removed the old Coin Detail card (renderCoinDetail safely no-ops without it).
  All cache-busters bumped v12 -> v13; new BINANCE-STYLE TRADE TAB CSS section.

DECISIONS MADE:
- Spot market data + futures-STYLE paper trading (per earlier agreement) — gives
  the Futures feel without needing the futures API or real mark/funding.
- Coins without a Binance USDT pair show a clear notice; the Trade Signal still
  works for them (it runs off CoinGecko data).

HONEST LIMITS (couldn't fully verify here):
- The TradingView embed + the Binance WebSocket can't be exercised in this
  sandbox (no browser, outbound WSS likely blocked). Syntax + the paper/signal
  math are tested; the live chart/book/trades need a quick look in a real
  browser. If Binance is geo-blocked on a viewer's network the feed shows a
  notice and the chart/signal still work.

STILL PENDING / NEXT TIME:
- PHASE 3: backend macro engine — "higher/lower than expected" via calendar
  forecast feeds, more sources (CryptoPanic/RSS/BLS/BEA/FRED/Treasury) + optional
  LLM reasons. Needs hosting + a few free API keys.
- Confirm phase 2 with Nimith (browser check), then deploy when he's ready.

===========================================
DATE: 6/19/2026 (Session 6 — macro/news Long/Short signal, phase 1)
-------------------------------------------
DONE TODAY:
- Reworked the Trade Signal so it always gives a clear directional call —
  LONG or SHORT, never "WAIT" — for the selected coin, with Entry, Stop Loss
  and three Take Profit targets shown as price, % move AND the $ profit/loss
  for a position size you type in (defaults to $1,000, updates live).
- New engine js/macro.js: reads the live classified news feed + Fear & Greed +
  the economic calendar and turns global macro events into a Long/Short bias
  using the trader cheat-sheet (lower inflation / rate cuts / more liquidity /
  dovish Fed / ETF approvals = bullish; higher inflation / hikes / less
  liquidity / hawkish Fed / crackdowns / war / financial stress = bearish).
  Topics covered: CPI/PCE, Fed/rates, NFP/jobs, GDP, ETF/adoption, regulation,
  liquidity, geopolitics, banking stress, and China/ECB/BoJ.
- Direction logic: macro/news LEADS, technicals (200/50-day MA, RSI, MACD)
  CONFIRM; if they conflict, confidence drops. Levels (entry/stop/targets) come
  from recent volatility + 30-day support/resistance.
- The signal shows "Why LONG/SHORT" — each reason links out to the actual
  headline it came from, tagged by category (e.g. "Inflation (CPI/PCE)").
  Upcoming high-impact events (FOMC/CPI within ~3 days) show as a volatility
  heads-up.
- Wiring: js/trade.js buildPlan() rebuilt to take the macro read and always
  pick a side; js/render.js renderTrade() rebuilt (levels + $ + linked reasons);
  js/app.js computes the macro read each 30s refresh and the position-size box
  re-computes the $ amounts. index.html got the position-size input + macro.js
  script; all cache-busters bumped v11 -> v12. CSS added under TRADE SIGNAL.
- Smoke-tested the engine in Node: bullish set (CPI cools, Fed cut, ETF inflows)
  -> LONG with links + FOMC heads-up; bearish set (hot CPI, hikes, SEC charges)
  -> SHORT even on a bullish chart, with lower confidence (technical conflict).

DECISIONS MADE:
- Phase 1 is browser-only and free, deploys to the existing site. It reads the
  DIRECTION a headline reports (e.g. "CPI cools", "Fed hikes"), NOT the surprise
  vs the consensus forecast — that needs the backend (phase 3).
- News currently comes from the CryptoCompare feed already wired in (covers
  CoinDesk, Cointelegraph, Decrypt, etc.). Bloomberg/WSJ/FT, the X accounts and
  paid on-chain (Glassnode/CryptoQuant) stay out until/unless we add a backend.
- Build order agreed with Nimith: do ALL three phases, confirm after each one.

STILL PENDING / NEXT TIME (build all, confirm each):
- PHASE 2: Binance-style Trade tab look — live TradingView chart, order book,
  recent trades, paper buy/sell.
- PHASE 3: backend macro engine — adds "higher/lower than expected" logic via
  economic-calendar forecast feeds (Forex Factory / Investing.com / Trading
  Economics), more sources (CryptoPanic, RSS, BLS/BEA, FRED, US Treasury) and
  optional LLM-written reasons. Needs hosting + a few free API keys.

===========================================
DATE: 6/18/2026 (Session 5c — hide password from inspect)
-------------------------------------------
DONE TODAY:
- Removed a real leak: auth.js had a code comment with the password in plain
  text (// sha256("...")). Deleted it. "View source" no longer shows it.
- Hardened the stored credential: instead of a plain SHA-256 (trivially cracked
  for a short numeric password via lookup sites), it's now a SALTED, KEY-STRETCHED
  hash — salt + 2000 rounds of SHA-256 (hashPassword() in auth.js). Inspect now
  shows only a salted hash that rainbow tables can't reverse. Login adds ~0.2-0.4s
  (one-time) which is fine. Still synchronous so it works from file://.
- Redacted the plaintext password from PROGRESS.txt (it is a public repo / the
  file is also served by Pages).
- Cache-busters bumped v10 -> v11 so the old auth.js (with the leak) can't stay
  cached in anyone's browser.

HONEST LIMITS (told Nimith):
- This is still a SOFT gate. A determined person can bypass it client-side
  (e.g., set the sessionStorage flag or read the page markup) regardless of how
  the password is stored. Snooping for the password via inspect is now defeated;
  true protection still requires backend auth (server/index.js) so the password
  and the protected content never reach the browser.
- The password also still exists in earlier git history. Removing that needs a
  history rewrite, or simpler: change the password (and ideally go backend).

STILL PENDING / NEXT TIME:
- Decide if we want real backend auth (the only way to make it truly private).
- Upload crypto-wallet photo as img/login-bg.jpg (see Session 5b).

===========================================
DATE: 6/18/2026 (Session 5b — login redesign)
-------------------------------------------
DONE TODAY:
- Redesigned the login screen into a split-panel layout (inspired by a reference
  Nimith shared): crypto photo + brand on the LEFT, sign-in form on the RIGHT,
  inside one rounded card floating on a dark gradient backdrop.
- Per Nimith: form has ONLY three things — a "User" field, a "Password" field,
  and a "Login" button. No name/email/terms/social buttons. Kept a small "Log in"
  title and a hidden error line (shows only on a wrong password).
- Left panel photo comes from img/login-bg.jpg. If the file is missing, the
  <img> removes itself (onerror) and a crypto fallback shows: dark gradient +
  a faint Lucide "wallet" glyph, with the CryptoPulse logo (top) and a tagline
  "Smarter crypto decisions." (bottom).
- New CSS classes: .login-aside / -img / -glyph / -overlay / -brand / -name /
  -tag, and .login-main / .login-title. Old single-card login styles replaced.
- Responsive: panels sit side by side on desktop; on <=720px the photo becomes
  a short banner on top and the form sits below.
- auth.js unchanged — kept the same element IDs (loginUser, loginPass, loginForm,
  loginError, loginCard, loginScreen), so admin / <the chosen password> and the session
  behavior still work.
- Cache-busters bumped v9 -> v10.

DECISIONS MADE:
- Kept the app's BLUE accent on the Login button for consistency with the
  dashboard (the reference was purple — offered Nimith the purple option).
- Photo: can't fetch/generate it here, so wired the slot + crypto fallback and
  suggested specific Unsplash/Pexels "crypto wallet" images to drop in.

STILL PENDING / NEXT TIME:
- Upload a crypto-wallet photo as img/login-bg.jpg (portrait/square, dark) to the
  live branch; it auto-deploys. Or send a URL and I'll wire it in.
- Optional: switch the login accent to the reference's purple if Nimith prefers.
===========================================
DATE: 6/18/2026 (Session 5 — login gate)
-------------------------------------------
DONE TODAY:
- Added a sign-in screen that gates the whole dashboard. Username "admin",
  password (the code Nimith chose). The app does not boot or fetch any data until login
  succeeds (not just a visual cover).
- New js/auth.js handles the gate. Password is stored as a SHA-256 hash (not
  plaintext); includes a self-contained SHA-256 so it also works from file://.
  Login is remembered for the browser session (sessionStorage). A "Log out"
  button was added to the top bar.
- js/app.js: removed the auto-boot; it now exposes CP.boot(), which auth.js
  calls after a successful sign-in. Guarded against double-boot.
- index.html: login screen markup + logout button; all cache-busters bumped
  v8 -> v9; auth.js script tag added (loaded after app.js).
- css/styles.css: new LOGIN GATE section — full-screen background photo (cover,
  centered, fits 4K screens), glass sign-in card, form styling, error shake,
  and a global .hidden utility.
- Background photo points to img/login-bg.jpg with a dark fallback until the
  file is added. img/README.txt documents the expected filename + hi-res advice.

DECISIONS MADE:
- Username + password both required (per Nimith).
- Stay logged in for the browser session, plus a Log out button (per Nimith).
- Honesty note: this is a SOFT gate, not real security. The site is public and
  static, so the check runs in the browser and can be bypassed via dev tools.
  The hash is obfuscation, not protection. Real auth would need the backend
  (server/index.js) hosted and authenticating server-side.

DEPLOYMENT (done this session):
- Found the cause of the earlier main-branch deploy failures: GitHub Pages here
  only deploys the repo's DEFAULT branch, and the default is
  claude/charming-fermat-9ht677 (not main). That's why main failed and that
  branch succeeded.
- Nimith said to just deploy it without him touching settings, so (with his
  go-ahead) the login-gate commit was fast-forwarded onto the live default
  branch claude/charming-fermat-9ht677 and pushed. Pages deploy run #9
  succeeded. Site is live at https://mnghouy-maker.github.io/Trade-Prediction/
- Note for next time: dev branch is claude/happy-feynman-mojcxx, but the LIVE /
  default branch is claude/charming-fermat-9ht677. To deploy, fast-forward
  charming-fermat to the dev commit (both are kept in sync). Long-term it would
  be cleaner to make one branch the single default — left as Nimith's call.

STILL PENDING / NEXT TIME:
- Upload the login background as img/login-bg.jpg (hi-res / ~4K). Until then the
  login screen uses the dark fallback. Easiest: drop it in the img/ folder on
  the default branch via GitHub and it auto-deploys; or tell me and I'll handle
  it and sync both branches.
===========================================
DATE: 6/16/2026 (Session 4 — coin detail panel + screener fixes)
-------------------------------------------
DONE TODAY (Session 4):
- Fixed markets.js error propagation: first-page failure now throws properly instead
  of silently resolving to [] and falling to sample data (was the root cause of
  Top 100 showing sample coins after the multi-page rewrite)
- Added 429 retry improvement: validates response is Array before returning
- Tools tab: added Coin Detail panel at the top — shows logo, name, rank, live price,
  24h/1h/7d changes, volume, market cap, signal score, 7d sparkline, and RSI/MA/MACD
  once full tech data loads
- Tools tab: added coin search box so user can look up any coin from the screener
  without leaving the Tools tab — shows dropdown suggestions, click to load detail
- Screener row click now navigates to Tools tab (was Trade tab) so user sees full
  coin info immediately
- All cache busters bumped to v7

DECISIONS MADE:
- Coin detail goes in Tools (not a new tab) to keep the navigation simple
- Tech indicators (RSI, MAs) load progressively — basic info shows instantly,
  full indicators appear once the 365-day chart finishes fetching

STILL PENDING / NEXT TIME:
- localStorage cache for market data (5-min TTL) to speed up Markets tab loads
- Push to GitHub

===========================================
DATE: 6/16/2026 (Session 3 — polish & bug fixes)
-------------------------------------------
DONE TODAY (Session 3):
- BTC/ETH logos added to price strip cards (fetched from CoinGecko static assets)
- Coin switcher: when altcoin selected from screener, shows logo + symbol badge
- Screener click now auto-switches to Trade tab so user sees the plan immediately
- "Reconnecting" bug fixed: priceTick() now calls tickStatus() after success
- Double-dot bug fixed: removed emoji (🟢 🟠) from status label text
- All emoji removed from UI text per CLAUDE.md rule 4 (render.js, alerts.js, app.js)
- Screener sample fallback expanded from 10 to 30 coins
- CoinGecko rate-limit retry: waits 8s and retries once on 429 response
- Tab switching extracted to reusable switchTab() function
- CSS v6: coin-btn-selected style, pc-logo sizing, fear-close SVG alignment

DONE TODAY (Session 2):
- Full tabbed navigation UI (v5):
    - 6 tabs: Dashboard, Markets, Analysis, Trade, News, Tools
    - Top tab bar on desktop, icon-only compact nav on mobile
    - Each section now focused and easy to read — no more single long scroll
- Dashboard tab:
    - Hero gauge + bull/bear score front and center
    - Live price strip: BTC, ETH, Fear & Greed, Market Cap — always visible
    - Signal overview grid below
    - Fear Alert banner — appears automatically when panic events detected
- Fear detection engine (sentiment.js):
    - Expanded keyword list covering exchange collapses, government bans,
      military conflict, bank failures, SEC charges, stablecoin depegs,
      liquidation cascades, war, sanctions, nuclear, rate hike surprises
    - Two-tier: FEAR_HIGH (phrases = +3 fear) and FEAR_MEDIUM (words = +1)
    - Fear Level score 0–100 shown in Sentiment tab
    - Fear override: if fearLevel >= 40, news signal forces bearish in score.js
    - Fear headlines flagged with orange warning tag in News tab
    - Fear items get left border highlight in news list
- Price strip added to Dashboard with live Binance 1s updates
- README fully rewritten to match new tabbed layout and fear detection feature
- All cache-busters bumped to v5

DECISIONS MADE:
- Tab labels hidden on mobile (icon only) to fit all 6 tabs on small screens
- Fear threshold set at 40/100 to trigger override — conservative enough to
  not false-positive on mild negative news
- No glassmorphism on cards (performance) — only on topbar

STILL PENDING / NEXT TIME:
- Nimith to review and give feedback on new layout
- Could add broader world news source (GNews/NewsData.io) for more fear signals
===========================================
DATE: 6/16/2026 (Session 1)
-------------------------------------------
DONE TODAY:
- Full CSS redesign (styles.css v4):
    - Font upgraded from system stack to Inter (Google Fonts)
    - Richer dark theme: deeper backgrounds, layered card depth with top-edge highlight and inner gradient
    - Hero section: larger score, glowing directional text (bull/bear/neutral glow), animated gauge fill with drop-shadow
    - Buttons: gradient primary, glow hover states
    - Cards: subtle glass-like top highlight, stronger shadows
    - Pills/badges: border + glow treatment
    - Tables: cleaner header styling, faint row separators, hover highlight
    - Custom thin scrollbars on news, history, screener
    - Animated background gradient on body
    - Live dot: pulsing ring glow
- Added Inter font via Google Fonts preconnect in index.html
- Bumped cache-buster to v4 on all CSS and JS script tags
- Sparkline charts (Option B) added to coin screener:
    - Mini 7-day SVG line chart with filled gradient area under the line
    - Green line for uptrend, red for downtrend
    - Drawn from the spark[] array already fetched from CoinGecko
    - New "7d Chart" column in screener table header
- Responsive improvements:
    - Phone (<640px): hero stacks to single column, logo shrinks, last-updated text hidden, screener hides 1h and sparkline cols to save space, calc grid single column
    - Tablet (960px): overview goes 3-col, hero adjusts to smaller gauge panel
    - Very small phones (<400px): graceful 2-col overview

DECISIONS MADE:
- Kept all existing JS logic untouched — only CSS and render.js renderScreener changed
- Sparkline downsamples the hourly spark array (every 4th point) for cleaner visuals
- No glassmorphism (backdrop-filter on cards) — kept performance safe, only used it on topbar

STILL PENDING / NEXT TIME:
- Nimith to review and give feedback
- Option to remove sparklines if not wanted
===========================================
